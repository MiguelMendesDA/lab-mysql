/*
As in the 'invoices' table, I executed the following command:
FOREIGN KEY (car_VIN) REFERENCES Sales_myCompany.cars(VIN)
And in the 'cars' table, I marked 'VNI' as UNIQUE. 
When seeding the table, MySQL didn't allow the insertion of duplicate data. 
Therefore, I cannot perform the intended task as I don't have duplicate data.
*/
